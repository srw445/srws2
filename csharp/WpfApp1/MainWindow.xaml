    <Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="加算電卓" Height="180" Width="360" WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8">数1:</TextBlock>
        <TextBox x:Name="Number1TextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"
                 PreviewTextInput="Number_PreviewTextInput" KeyDown="NumberTextBox_KeyDown" />

        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8">数2:</TextBlock>
        <TextBox x:Name="Number2TextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"
                 PreviewTextInput="Number_PreviewTextInput" KeyDown="NumberTextBox_KeyDown" />

        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,4,0,8">
            <Button x:Name="AddButton" Width="100" Click="AddButton_Click">加算</Button>
            <Button Width="80" Margin="8,0,0,0" Click="ClearButton_Click">クリア</Button>
        </StackPanel>

        <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" x:Name="ResultTextBlock"
                   FontSize="16" FontWeight="Bold" Text="結果: " VerticalAlignment="Top" />
    </Grid>
</Window>
